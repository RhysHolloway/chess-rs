[package]
name = "chess-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
# hashbrown = { workspace = true }
serde = { optional = true, workspace = true }
# rhai = { workspace = true }

[features]
# default = ["serde"]
